import type { LogoId } from '@/types';

/** Auswählbare Firmenlogos (Symbole). */
export const LOGO_IDS: LogoId[] = ['cpu', 'zap', 'rocket', 'atom', 'hexagon', 'circuit', 'orbit', 'shield', 'layers', 'sparkles', 'gem', 'globe'];
